# 统一换行符为 LF，避免 Windows 下 git 自动转换导致的 CRLF 抖动
* text=auto eol=lf

# 二进制文件不做行尾处理
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg binary
*.woff binary
*.woff2 binary

# Windows 批处理脚本保留 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
