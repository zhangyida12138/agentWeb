# 应用
APP_NAME=AgentWeb API
APP_ENV=local
DEBUG=true

# 允许跨域的前端地址（JSON 数组）
CORS_ORIGINS=["http://localhost:5173"]

# PostgreSQL（与仓库根目录 docker-compose.yml 保持一致）
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/agent_web
DATABASE_ECHO=false
